package fly.factions.api.exceptions;

/**
 * The {@code NotAMemberException} class is thrown when a user who isn't a member of a group attempts to do a group member only action.
 */
public class NotAMemberException extends RuntimeException {
}
